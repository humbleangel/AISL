# Swarm review v3 — new-domain (additive, non-destructive)

v1 folders + v2 file kept. This file adds the cross-swarm v3. DOMAIN owns buyer-facing shapes only; mechanisms/verdicts owned outward.

## Collisions resolved
- vt-x-fuzz-lab vs interface/abundance/substrate/scale/oracle: INTERFACE owns demo primitive, SUBSTRATE owns EPT mechanism, ABUNDANCE owns per-actor pattern, SCALE owns clone, ORACLE owns replay verdict; DOMAIN keeps fuzz-lab as buyer-facing lab composing them.
- trust-bootstrap-rig: DROPPED from domain. ORACLE owns verdict, SUBSTRATE owns mechanism, ABUNDANCE owns chain, ORGANIZATION owns log, FAILURE owns threat, SCARCITY owns budget.
- tpm-sealed-inference-node + offline-signing-vault vs oracle/observer/substrate: SUBSTRATE owns seal/measure, ORACLE owns verdict, OBSERVER owns quote; DOMAIN owns deployed shapes as markets.
- ai-agent-sandbox vs organization/abundance/observer: ABUNDANCE owns isolation, OBSERVER owns dissent check, ORGANIZATION owns classroom track; DOMAIN owns sandbox as paid hostile-agent containment product.
- disposable-ci-worker vs abundance/scale/pairing: ABUNDANCE owns disposable pattern, SCALE owns single-task shape, PAIRING owns build-attestation link; DOMAIN owns CI-worker SKU.
- recovery-rescue-image vs scale/time/failure/substrate: those own boot/resume/crash mechanisms; DOMAIN owns rescue-image as distribution Trojan-horse.

## New insights
- No v2 sells heat: first sellable SKU is joules — thermal-metered batch furnace where Linux throttles dishonestly and AISL proves budget.
- No v2 sells powercuts: storage/network vendors pay for an honest powercut-proving rig before trusting AISL for anything else.
- Rescue is the inverse beachhead: aid the dead Linux box instead of replacing the live one — no install friction, pure resume-not-boot demo.
- Sealed inference + signing are one metal truth: TPM + no-shared-memory + speculation-secrecy makes small high-value secrets (keys, weights, ballots) the right first market.
- E-waste is the fab: no-fab x86-64 + minimal-measured-boot turns retired ThinkPads/NUCs into trust rigs; degraded operation is a beachhead systemd cannot follow.

## Completed sublist v3
- ai-agent-sandbox: EPT-isolated VM-per-agent where faults-are-control-flow contains untrusted agents without Linux containers.
- disposable-ci-worker: single-task-no-scheduler attested worker that boots, builds, quotes PCRs, dies — no daemon persistence to poison.
- recovery-rescue-image: resume-not-boot USB that mounts dead NVMe as persistent pages, proving AISL on enemy hardware without install.
- tpm-sealed-inference-node: TPM-sealed weights + no shared mutable memory serve models where even root cannot exfiltrate keys.
- offline-signing-vault: airgapped ceremony box where caller-allocates + crash-only pages make key signing replayable and powercut-safe.
- vt-x-fuzz-lab: rented adversary cell composing EPT partitions + trapped-replay + sibling-core dissent to hunt speculation leaks.
- powercut-proving-rig: NVMe powercut torture bench selling nvme-powercut-recovery proofs storage vendors cannot get from POSIX fs.
- thermal-metered-batch-node: AVX batch furnace selling RAPL-metered joules with power-as-capability enforcement, not cgroup promises.
