# Brutal combine v1 — new-domain (additive, non-destructive)

Refiner lineage kept. Force-merged against all 15 other v5s. Domain owns buyer-facing shapes only.

## Merges and kills
- ept-fuzz-partition KILLED as standalone SKU (folded into agent-cell as fanout-mode — same buyer shape, modes not SKUs).
- joule-capped-batch-node, minimal-resume-image killed as shapes (lease property / time property, not buyer shapes).
- powercut rig stays outward (oracle, not shape).
- Names scrubbed of mechanism leakage (no ept-/tpm-/pku-/pt-/rapl- in buyer definitions — those map to subtraction/oracle verdicts buyers pay for).

## Final merged list (5 survive)
- dmaless-signing-vault: high-$ beachhead; keys signable only via live quorum, no DMA exfil path, slow tolerated.
- disposable-ci-worker: volume reproducible runs; crash-only, no files/processes, microsecond resume.
- sealed-inference-node: regulated attested inference; pinned expiry with outward quorum gate, no wall-clock.
- disposable-agent-cell: untrusted-agent containment with bounded trace and veto-by-live-quorum; includes fuzz-fanout mode.
- sealed-recovery-escrow: noncustodial deadman recovery; sealed successor with attested append log, drill-gated.
