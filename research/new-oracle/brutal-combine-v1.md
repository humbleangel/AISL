# Brutal combine v1 — new-oracle (additive, non-destructive)

Refiner lineage kept. Force-merged against all 15 other v5s. Oracle = verdicts only.

## Merges and kills
- scrub-trail-verdict KILLED as oracle duty (writing trail violates crash-only + endurance + flush-lie; ceded to observer; oracle reads witness hash only).
- Outward-gate batched by joule-epoch, fail-closed (per-verdict TPM/IPI quorum is ms-latency fiction).
- Replay verdict requires bounded filtered-PT slice (lossless PT is fiction).
- Tripwire downgraded veto-only (PMU spoofable, never proof).
- Microcode-expiry kept with re-quote-or-fault (no grace).
- Causal-bind kept as cite-only (no global VA/now; local arena + maxtick).
- Degraded-physics kept as quorum input with dual timer+joule lease (skew = dissent).

## Final merged list (6 survive)
- outward-quorum-gate-verdict: outward verdict exists only as live-IPI quorum + signed wall-range epoch, fail-closed otherwise.
- dissenter-replay-verdict: verdict counts only if two live dissenters can re-check it from bounded filtered-PT slice.
- speculation-tripwire-verdict: any uninvited-speculation witness forces fault; same-core zero/scrub proof required on use.
- microcode-pinned-expiry-verdict: verdict dies on microcode/SMM/measured-page drift; sparse TPM epoch re-pin or fault.
- causal-cite-verdict: verdict cites EPT-cap + bounded-COW causal index + maxtick; binds nothing, no global now.
- degraded-physics-quorum-verdict: joule/PMU/degraded-core inputs vote as physics quorum with dual timer+joule lease; skew is dissent.
