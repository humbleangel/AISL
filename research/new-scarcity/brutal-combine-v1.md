# Brutal combine v1 — new-scarcity (additive, non-destructive)

Refiner lineage kept. Force-merged against all 15 other v5s. Scarcity owns what-is-scarce.

## Merges and kills
- scrub-containment-residue KILLED as standalone: no independent meter apart from joules/lanes/proof — cost already owned; residue is a verdict, not a budget.
- All 6 survivors confirmed with independent meters: diff-lines + live-quorum veto (review); RAPL lease + PMU judge (joules); resident-ceiling + INVVPID accounting (TLB); append-trail counter (commits); epoch gate (quotes); bytes-per-lane + PTWRITE filter (trace).

## Final merged list (6 survive)
- review-attention-bandwidth: only non-renewable; all machine verdicts pre-filter so humans merge diffs only.
- joule-thermal-headroom: RAPL-leased energy/thermal ceiling pays for compute, AVX, scrub, precompute.
- nested-ept-tlb-budget: STLB/EPT-walk/PCID/VPID ceiling caps isolation fanout and resident views.
- durable-commit-endurance: P/E-finite NVMe budget; snapshots cheap, commits leased and capped.
- quote-attestation-bandwidth: 20–100ms TPM quotes forced to sparse epochs anchoring measured execute.
- trace-replay-bandwidth: PT-GB/s/LBR-32 forced to filtered PTWRITE witness with per-lane byte budget.
