# Brutal combine v1 — new-interface-to-intent (additive, non-destructive)

Refiner lineage kept. Force-merged against all 15 other v5s. Bun rule enforced: interface drafts checkable faults/diffs only; all verdicts outward.

## Merges and kills
- Veto + ambiguity fused (both fail-closed outward — one entry).
- Seal + undo fused (both pinned CoW resume — one entry).
- Per-resume TPM seal killed (ms vs microseconds); wall-windows killed (no-global-now); coherent heap killed; trusted firmware killed; string authority killed.

## Final merged list (6 survive)
- ept-cap-references-only: holder is EPT/PKU cap, kills strings/fds/ambient per Thompson distrust.
- forbid-pages-permit-measured-code: default forbid, language fault + measured anchor permits, kills unmeasured exec.
- disposable-cell-demo-bounded-pt: one-shot EPT cell with per-lane PT + joule deadline, kills persistent unbounded demo.
- proposer-drafts-checker-judges-bun-split: interface drafts syntax/faults only, external checker/quorum judges, never same.
- live-quorum-veto-fault-outward-fail-closed: live IPI/PIR ballot + ambiguity faults outward, timeout/skew = dissent/fail-closed not retry.
- pinned-root-diff-resume-cow-index: TPM pins root once, every resume diff-checks to bounded CoW causal index, kills per-resume seal and wall-windows.
